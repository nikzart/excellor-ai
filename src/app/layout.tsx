import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EXCELLOR AI - Your UPSC Preparation Assistant",
  description: "Advanced AI-powered platform for UPSC preparation, current affairs, and general knowledge. Get personalized study assistance and stay updated with the latest developments.",
  keywords: "UPSC, IAS, civil services, current affairs, general knowledge, AI assistant, exam preparation",
  authors: [{ name: "EXCELLOR AI Team" }],
  robots: "index, follow",
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
